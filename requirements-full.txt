# Everything in requirements.txt, plus optional integrations.
-r requirements.txt

# Sentiment trading strategy (SentimentAnalysisTrader)
tensorflow>=2.15,<3
newsapi-python>=0.2.7

# Persistence
SQLAlchemy>=2.0
psycopg2-binary>=2.9     # only needed if DATABASE_URL points at PostgreSQL

# Event streaming
redis>=5.0
confluent-kafka>=2.3

# Parameter search / experiment tracking
optuna>=3.5
mlflow>=2.10
